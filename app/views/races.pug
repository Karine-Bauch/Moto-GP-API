extends layouts/main.pug

block append header
    include partials/header.pug

block append main
    section
        h2.title All races

        each race in races
            small.primary= race.name + ' - ' + race.date.toLocaleDateString()
            br


block append footer
    include partials/footer.pug
