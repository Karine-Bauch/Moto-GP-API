extends layouts/main.pug

block append header
    include partials/header.pug

block append main
    section
        h2.title All racetracks

        each racetrack in racetracks
            small.primary= racetrack.name + ' at ' + racetrack.city + ' in ' + racetrack.country
            br


block append footer
    include partials/footer.pug
